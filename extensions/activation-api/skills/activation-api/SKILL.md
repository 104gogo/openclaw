---
name: activation-api
description: Use get_activation_code tool to fetch activation codes from the configured external API.
---

When the user asks for an activation code, activation key, or mentions "激活码" / "激活密钥", use the `get_activation_code` tool to fetch the current code from the configured API.

Do not attempt to generate or guess the code — always call the tool to get the real value.
