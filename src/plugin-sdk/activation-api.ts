// Narrow plugin-sdk surface for the bundled activation-api plugin.
// Keep this list additive and scoped to symbols used under extensions/activation-api.

export type {
  AnyAgentTool,
  OpenClawPluginApi,
  OpenClawPluginCommandDefinition,
  PluginCommandContext,
} from "../plugins/types.js";
